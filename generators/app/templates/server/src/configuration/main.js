const fonts = require('./fonts');
const layers = require('./layers');

const configuration = {
  card: {
    width: 1000,
    height: 400,
    fill: '#333333'
  },
  fonts: fonts,
  layers: layers
}

module.exports = configuration;